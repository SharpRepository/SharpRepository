{
  "sharpRepository": {
    "repositories": {
      "default": "couchDb",
      "couchDb": {
        "factory": "SharpRepository.CouchDbRepository.CouchDbConfigRepositoryFactory, SharpRepository.CouchDbRepository",
        "host": "127.0.0.1", // optional 127.0.0.1 is the default
        "port": "5984", // optional 5984 is the default
        "username": "user", // optional
        "passord": "pw" // optional
      }
    }
  }
}